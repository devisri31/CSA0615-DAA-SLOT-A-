arr = [14, 9, 22, 35, 18, 41, 27]
key = 18

# Ordinary Search
count1 = 0
for i in range(len(arr)):
    count1 += 1
    if arr[i] == key:
        pos = i + 1
        break

# Sentinel Search
temp = arr.copy()
temp.append(key)

count2 = 0
i = 0
while temp[i] != key:
    count2 += 1
    i += 1
count2 += 1

print("Position found =", pos)
print("Ordinary comparisons =", count1)
print("Sentinel comparisons =", count2)
